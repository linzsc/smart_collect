# UI 树优先方案：技术选型说明（2026-08）

> 分支：codex/ui-tree
> 目的：说明"为什么主流 agent 视觉优先，而我们在这个采集场景反着来——UI 树优先"，作为本分支的技术选型依据。
> 配套：output/ui_tree_analysis_2026-08-05.md（7 页面真机实测）、异常处理落地执行计划-2026-08.md

---

## 一、主流 agent 方案为什么"视觉优先"

主流 GUI agent（UI-TARS / Operator-CUA / AppAgent / Auto-UI 等）普遍以**截图 + VLM** 为感知主路径，原因：

| 原因 | 说明 |
|---|---|
| **泛化性** | VLM 看截图可在任意 App 工作（游戏/WebView/自绘/iOS/桌面/浏览器）；UI 树平台绑定（uiautomator/XCUITest/DOM/a11y），且 WebView/自绘/安全页经常拿不到 |
| **鲁棒性** | 截图永远反映真实渲染；UI 树有空 dump、无障碍权限、ROM 差异、动画中 stale bounds 等失败模式 |
| **训练对齐** | 现代 GUI agent 以"截图-动作"对训练，模型天生在像素上 grounding；XML 是另一模态，输入更噪 |
| **部署简单** | 纯视觉不需要无障碍权限，跨平台/云手机都能跑 |
| **成本权衡** | 反正规划仍需 VLM，多一条 UI 树路径 = 多维护 + 多失败点 |

> 注：主流并非完全不用结构信号——Auto-UI（view hierarchy+截图）、AppAgent（a11y dump 探索）、Claude for Android（无障碍树+截图）都是**混合**方案；只是视觉优先是近年趋势。

## 二、为什么我们在这个场景反着来：UI 树优先

| 维度 | 主流 agent | 本项目（智能采集） |
|---|---|---|
| 任务形态 | 任意 App、一次性 | **单一 App（高德）、长流程（几百步）** |
| 优化目标 | 通用性 > 成本 | **确定性、成本、时延** |
| 每步 LLM 预算 | 可接受 | **0–1 次/任务** |
| 结构化需求 | 低 | **高**（字段/状态/位置要精确） |

**实测数据支撑（2026-08-05，7 页面）**：
- 6/7 页面 UI 树完整（home/打车/POI/冒泡/详细页×2），只有**计价弹窗间歇性空树**（node=1 → node=27）；
- **冒泡页供应商行 content-desc 是结构化数据**：`车型:XX 已选/未选 预估 XX 元` → S2 识别、勾选状态、价格全部直接可读（0 VLM / 0 像素分类）；
- **详细页 174/146 个文字节点**：起步价/里程费/时长费/远途费/跨城费等字段直接可读（0 OCR 且更准）；
- 每个节点带 bounds 中心 → 定位 0 VLM/OCR。

**结论**：在这个场景，UI 树的结构化收益（省 LLM/OCR、字段精确）**远大于**通用性需求，因此选择 UI 树优先。

## 三、分层架构（混合，不是二选一）

```
UI 树（原生页面，首选）—— 文字/字段/状态/位置，0 LLM 0 OCR
   ↓ 空 dump / WebView / 无文字图标
OCR bbox（任何渲染，兜底）—— 文字+坐标，0 LLM（<50ms）
   ↓ 需要视觉语义 / 未知异常
VLM（LLM，最后兜底）——— 图标定位 + L3 恢复策略生成（0–1 次/任务）
```

- **页面判别**：UI 树文字集合 vs 页面目录（required/forbidden）→ 0 OCR；
- **S2 供应商识别**：解析 `车型:XX 已选/未选` desc → 0 VLM；
- **勾选状态**：desc"已选/未选" → 0 像素分类；
- **预约用车锚点**：UI 树 text → 0 OCR；
- **字段读取（详细页）**：直接读节点 → 0 OCR 且更准；
- **计价弹窗**：UI 树重试 → 空则 OCR 兜底（唯一已知盲区）。

## 四、已知风险与缓解

| 风险 | 缓解 |
|---|---|
| `uiautomator dump` 慢（~0.5–2s） | 按需 dump + 缓存（页面变化才重 dump）；只在关键步骤用 |
| 空 dump / 间歇（计价弹窗） | dump 重试（≥2 次）→ OCR 兜底 |
| WebView/H5/自绘/安全页无树 | OCR 兜底；VLM 最后兜底 |
| 富文本/base64 噪音 | 清洗规则（`tools/ui_tree_collect.py` 已验证） |
| 「?」问号不在 UI 树 | 行 bounds 几何推导（行内右侧区域）或 ref_image/VLM 兜底 |
| 动态内容（价格/供应商名） | 只做行级定位，不做页级判别 |

## 五、决策结论

1. 本场景 **UI 树优先**，OCR 兜底 WebView/空树，VLM 只留最后兜底——与主流 agent 视觉优先不矛盾，是"通用性 vs 结构化收益"在不同目标下的正确取舍；
2. 实施顺序（见异常处理落地执行计划 P0）：
   - ① S2 供应商识别改 UI 树 desc 解析（收益最大、替换最干净）；
   - ② 页面判别改 UI 树文字（0 OCR）；
   - ③ 勾选状态读 desc（0 像素分类）；
   - ④ 详细页字段读取（0 OCR）；
   - ⑤ 计价弹窗保留 OCR 兜底。

---

## 六、不需要硬编码坐标：相对几何 + 动态列聚类

**行归属**：用节点自己的 bounds 做相对比较（不写死像素值）：
```
经济型标题 y ← 「经济型·N」节点 bounds 动态读
下一类别标题 y ← 下一标题节点动态读
运力商行 y ← 行节点（clickable 且 desc ^车型:）自己的 bounds
规则 = 行.y ∈ [标题A.y, 标题B.y) → 自适应分辨率/滚动位置
```

**区分"左侧栏 vs 列表区"**：唯一像阈值的地方，用动态列聚类解决：
- 把"类别名文字"的 x 聚成列；出现最多的列 = 左侧栏（7 个类别全在 x≈91），其余 = 列表标题 → 不写死 x。

## 七、经济型 14 个：必须滚动累计，「经济型·N」当完成判据

- `uiautomator dump` 只返回当前屏可见节点（本屏仅 5 个经济型，其余 9 个在屏外）→ **必须滚动 + 每屏 dump + 去重累计**（复用 s4_scroll / _processed 语义）；
- **完成判据**：`经济型·14` 的 N 是 App 报的经济型总数 → 采满 N 即停（可动态替代 max_suppliers=10）；或列表区出现 特快车/出租/优享/专车/六座 标题（economy_ended）；或滚动到底无新增（兜底）。

## 八、问号 / 勾选框定位（冒泡页实测）

**发现**：运力商行内的「?」和「勾选框」都是**可点击但无文字/desc** 的节点，无法按名找，但可**按行相对几何定位**：

| 元素 | 实测位置 | 定位规则（动态） |
|---|---|---|
| 运力商行 | clickable + desc `车型:XX 已选/未选 预估 XX 元` @ x≈691 | 直接匹配 desc |
| 「?」按钮 | clickable ViewGroup @ x≈864（行中心与「预估」之间），无 desc | 行 y 带内，x ∈ (行中心, 「预估」文本 x) 的可点击节点 |
| 勾选框 | clickable ImageView @ x≈1135（「预估」右侧最右），无 desc | 行 y 带内，x 最大的可点击节点（价格右侧） |

- 「?」点击：`预估` 文本位置从 UI 树读 → 行内取"行中心~预估之间"的可点击节点点击 → 0 LLM；
- 勾选状态：直接读行 desc「已选/未选」→ 未选则点勾选框（价格右侧最右可点击节点）→ 再验 desc → 场景 C 恢复链变为全确定性；
- 兜底：若某行「?」节点缺失（布局变化），用 ref_image（button_to_price.png）或 VLM 最后兜底。

**结论**：运力商识别 + 点「?」 + 勾选框修复，**全部可确定性实现，0 LLM 0 OCR**（除计价弹窗 WebView）。
