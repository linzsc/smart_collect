# 移动端打车截图采集 Demo 发展计划

> 版本：2026-08-03  
> 当前目标：先在真机上稳定生产完整、低重复的计价截图，再接入线上识别链路，通过数据 Diff 判断最终产出是否可用。

## 1. 项目定位

本项目不是一个让 Agent 自主完成全部操作的通用 Mobile Agent，而是一个生产级、确定性优先的移动端截图采集执行器：

```text
确定性流程编排
  + 稀疏 VLM 视觉定位
  + 闭环滚动与关键帧采集
  + 状态验证
  + 受限异常恢复 Agent（后续阶段）
```

项目边界：

- 本项目负责打开 App、输入起终点、进入计价页面、遍历运力商并生产截图。
- 已有线上服务负责截图识别和数据落库，本项目不重复建设识别服务。
- 本项目需要提供线上识别接入客户端和质检 Diff，以验证截图最终能否稳定产生正确数据。
- 正常采集路径不使用 Agent；只有确定性恢复策略无法处理的未知异常才唤醒 Agent。

## 2. 当前 Demo 状态

### 2.1 已具备

- 小米真机 ADB 控制和截图能力。
- YAML 配置的确定性流程编排。
- 打开高德、进入打车页、输入起终点。
- 使用 Qwen3.5-VL-Plus 完成关键元素 Grounding。
- 遍历经济型运力商并进入详细计价规则。
- 工作日、休息日页面滑动和截图。
- 基础运行日志、截图标注和调用次数统计。

### 2.2 当前主要缺口

| 优先级 | 缺口 | 影响 |
|---|---|---|
| P0 | 详情页仍按固定次数滑动并大量截图 | 截图重复、成本高，无法证明内容无遗漏 |
| P0 | 终止条件尚未改成蓝色“预约用车” | 可能提前停止或滑过目标位置 |
| P0 | 手势距离与页面实际位移未闭环 | 不同页面、设备、帧率下滚动距离不稳定 |
| P0 | 关键步骤缺少统一 StateVerifier | 点击失败、跳错页面后可能继续执行 |
| P1 | 尚未形成标准 Capture Manifest | 截图、场景、运力商、标签页和识别结果难以关联 |
| P1 | 线上识别链路尚未完成联调 | 无法判断截图最终数据质量 |
| P1 | 尚未建立 Ground Truth 和数据 Diff | 无法区分采集问题与识别问题 |
| P2 | 异常案例没有统一记录和沉淀 | 相同问题会重复依赖人工分析 |
| P2 | Recovery Agent 尚未形成受限执行接口 | 未知页面无法自动恢复 |

## 3. 目标架构

```text
Job / 场景参数
       ↓
FlowEngine（确定性 SOP）
       ↓
关键节点 VLM Grounding
       ↓
StateVerifier
       ↓
AdaptiveDetailCollector
  ├─ 本地 OCR：检测“预约用车”
  ├─ 位移估计：OCR 锚点 / OpenCV 重叠匹配
  ├─ 自适应滑动：控制下一次手势距离
  └─ KeyframeSelector：只保存有新内容的截图
       ↓
Capture Manifest + 原始关键帧
       ↓
线上识别服务
       ↓
QC Diff（识别结果 vs Ground Truth）
```

异常分支：

```text
StateVerifier 失败
  → 固定重试
  → 已知 RecoveryPolicy
  → 未知异常 Recovery Agent
  → 仍失败则停止并保存异常现场
```

## 4. 三阶段发展计划

### 阶段一：本地闭环采集稳定化（第 1 周）

目标：真机本地运行时，用尽量少的截图完整覆盖详情内容，并在出现“预约用车”时可靠停止。

#### 工作项

1. 新增 `EndMarkerDetector`
   - 每张候选截图使用本地 OCR 检测“预约用车”。
   - 精确文本直接命中；模糊文本结合蓝色区域特征确认。
   - Qwen 只在 OCR 不确定或达到最大滑动次数时兜底。

2. 新增 `ScrollDisplacementEstimator`
   - 使用前后截图中相同 OCR 文本框的纵向位置差估算实际位移。
   - OCR 锚点不足时使用 OpenCV 垂直重叠匹配。
   - 排除吸顶标题、状态栏、底部固定按钮等非滚动区域。

3. 新增 `AdaptiveScroller`
   - 使用 700～1000ms 慢速拖动，降低惯性影响。
   - 初始目标位移为有效内容区高度的 55%～65%。
   - 根据实测页面位移调整下一次手势距离。
   - 保证相邻正式截图至少有 20%～30% 重叠。
   - 重叠不足时执行小距离反向滚动并补充桥接帧。

4. 新增 `KeyframeSelector`
   - 探测帧只使用临时文件，不进入最终产物。
   - 同一候选截图同时用于终点检测、重叠检测和正式采集。
   - 仅在新内容比例超过阈值时保存为关键帧。
   - 最后一张包含“预约用车”的截图必须保留。

5. 增加基础 `StateVerifier`
   - 进入打车页后验证页面状态。
   - 点击问号后验证计价弹窗。
   - 点击详细计价规则后验证详情页。
   - 切换工作日、休息日后验证标签状态。

#### 阶段验收条件

- 连续执行至少 30 个详情页采集，无内容断层。
- “预约用车”终止标志漏检率为 0。
- 相邻关键帧均能验证存在重叠区域。
- 最终正式截图数相对当前方案减少至少 50%。
- 工作日、休息日和最终终止截图均可正确关联。
- 异常情况下不会无限滑动，达到上限后能够安全退出并保留现场。

### 阶段二：线上识别联调与数据 Diff（第 2 周）

目标：把本地采集产物送入现有线上识别服务，并通过 Diff 定位最终数据质量问题。

#### 工作项

1. 定义 Capture Manifest

每次采集至少记录：

```json
{
  "run_id": "...",
  "scenario_id": "...",
  "app_version": "...",
  "sop_version": "...",
  "pickup": "...",
  "dropoff": "...",
  "supplier": "特惠快车",
  "tab": "工作日",
  "frame_index": 2,
  "image_path": "...",
  "overlap_ratio": 0.27,
  "new_content_ratio": 0.73,
  "end_marker_found": false
}
```

2. 接入线上识别服务
   - 使用 `run_id + supplier + tab + frame_index` 建立请求关联。
   - 增加超时、重试、幂等和原始响应保存。
   - 保留截图、请求参数、识别响应的完整追踪关系。

3. 准备第一版测试集
   - 先选择 30～50 个代表性场景。
   - 覆盖不同起终点、不同运力商、长短详情页、工作日和休息日。
   - 包含首屏即接近终点、需要多次滑动、网络较慢等情况。
   - 每个场景人工确认截图是否完整，并标注关键字段 Ground Truth。

4. 实现 QC Diff
   - 对金额、补贴、运力商名称、工作日/休息日规则进行标准化。
   - 输出缺失字段、多余字段、数值不一致、重复记录和规则文本差异。
   - 将问题归因到采集、截图覆盖、识别或数据合并四类。

#### Diff 关注指标

| 指标 | 说明 |
|---|---|
| 截图覆盖完整率 | Ground Truth 内容是否全部出现在关键帧中 |
| 终止检测召回率 | 出现“预约用车”时是否可靠停止 |
| 运力商完整率 | 应采运力商是否全部产出 |
| 字段完整率 | 价格、补贴、计价规则是否缺失 |
| 字段准确率 | 识别值与人工标注是否一致 |
| 重复记录率 | 重叠截图是否造成重复落库 |
| 场景级通过率 | 一个场景的全部关键字段是否通过 |

#### 阶段验收条件

- 本地截图能够自动提交线上识别服务。
- 每张截图均能追踪到对应场景、运力商和标签页。
- 30～50 个场景均生成结构化 Diff 报告。
- 可以明确区分“截图没拍到”和“拍到了但识别错误”。
- 重叠截图产生的重复数据能够被检测出来。

### 阶段三：异常恢复和策略沉淀（第 3 周及以后）

目标：先处理高频已知异常，再验证受限 Recovery Agent 是否能覆盖未知页面。

#### 工作项

1. 建立 Incident 数据结构
   - 保存预期状态、实际截图、最近动作、失败步骤和 App/SOP 版本。
   - 异常发生时保留前一帧、当前帧和关键日志。

2. 实现已知 RecoveryPolicy
   - 权限弹窗、更新提示、定位失败、网络重试等先写成确定性规则。
   - 成功恢复后重新执行原步骤并由 StateVerifier 验证。

3. 定义受限 Recovery Agent 接口
   - Agent 只负责把页面恢复到当前步骤的预期状态。
   - 仅开放返回、等待、重新打开 App、滚动和安全目标点击。
   - 最多执行 3～5 步，每步都经过 SafetyGuard 和 StateVerifier。

4. 策略沉淀
   - Agent 首次成功只保存为候选 Incident。
   - 经过人工审核或多次回放成功后升级为 RecoveryPolicy。
   - 按 App 版本和 SOP 版本限制策略有效范围。

#### 阶段验收条件

- 已知异常优先由确定性规则恢复，不调用 Agent。
- Agent 无法执行叫车、支付、登录等高风险动作。
- 每个异常都有完整上下文和恢复结果。
- Agent 成功案例可以被审核并沉淀为后续固定策略。

## 5. 建议的 15 个工作日安排

| 时间 | 任务 | 当日/阶段产物 |
|---|---|---|
| D1 | 固化采集 SOP、详情页有效 ROI、Manifest 字段 | SOP v1、Manifest Schema |
| D2 | 实现本地“预约用车”OCR检测 | EndMarkerDetector、样例测试 |
| D3 | 实现 OCR 锚点位移估计和图像匹配兜底 | DisplacementEstimator |
| D4 | 实现自适应滑动控制和慢速拖动 | AdaptiveScroller |
| D5 | 实现关键帧筛选、临时探测帧和终点保留 | KeyframeSelector |
| D6 | 真机运行 10～15 个场景并修正参数 | 本地运行记录、参数基线 |
| D7 | 扩展至 30 个详情页，人工检查内容连续性 | 本地验收报告 |
| D8 | 实现线上识别服务客户端和请求追踪 | RecognitionClient |
| D9 | 完成批量提交、重试、幂等和响应保存 | 端到端联调样例 |
| D10 | 定义 Ground Truth 和字段标准化规则 | 标注模板、Normalization |
| D11 | 实现结构化数据 Diff | Diff JSON/CSV 报告 |
| D12 | 完成 30～50 个场景批量测试 | 第一版质量报告 |
| D13 | 按 Diff 结果修复采集或截图覆盖问题 | 修复版本、回归结果 |
| D14 | 增加 Incident 记录和两三个已知恢复策略 | IncidentStore、RecoveryPolicy |
| D15 | 汇总 Demo、指标、问题归因和下一阶段计划 | Mentor 演示包和汇报材料 |

## 6. 推荐代码拆分

```text
collector/
  flow_engine.py                 # 保留：正常确定性流程
  ride_pricing.py                # 调用新的详情采集器
  detail_capture/
    end_marker_detector.py       # “预约用车”检测
    displacement_estimator.py    # 实际滚动位移估计
    adaptive_scroller.py         # 闭环滑动控制
    keyframe_selector.py         # 重叠检测和关键帧筛选
    manifest.py                  # Capture Manifest
  verification/
    state_verifier.py            # 关键页面状态验证
  integration/
    recognition_client.py        # 线上识别服务客户端
  quality/
    normalizer.py                # 字段标准化
    diff_engine.py               # 数据 Diff
  recovery/
    context.py                   # 异常上下文
    policy.py                    # 已知恢复策略
    agent.py                     # 后续受限 Agent 接口
```

不建议立即重写现有 FlowEngine。优先把 `_scroll_to_bottom()` 替换为新的详情采集器，并通过小接口逐步接入其他能力。

## 7. 当前阶段暂缓事项

以下内容不属于当前 Mentor 验收的关键路径，暂时不投入主要开发时间：

- Temporal 或复杂分布式工作流。
- 多手机集群调度。
- 完整接入 Mobile-Agent v3.5。
- LangGraph 长期记忆和人工中断。
- 本地 Grounding 模型选型与微调。
- Go 重写 ADB 控制层。

这些工作应在“本地采集 → 线上识别 → 数据 Diff”证明最终产出可用后再决定。

## 8. Mentor 演示与交付清单

最终演示建议固定展示同一个场景的完整证据链：

1. 输入起点和终点，启动真机采集。
2. 展示关键步骤由 VLM 定位，滑动等机械动作不调用 LLM。
3. 展示详情页根据实际位移自适应滑动。
4. 展示使用最少关键帧覆盖完整内容。
5. 展示识别到“预约用车”后停止。
6. 将截图提交线上识别链路。
7. 展示识别结果与 Ground Truth 的结构化 Diff。
8. 展示一个异常被记录、恢复或安全停止的案例。

建议交付：

- 可运行 Demo 和配置文件。
- 30～50 个场景测试集。
- Capture Manifest 与关键帧截图。
- 线上识别原始输出。
- QC Diff 报告。
- 本地采集指标汇总。
- 已知问题、风险和下一阶段计划。

## 9. 最重要的决策顺序

```text
先证明截图完整且低重复
    ↓
再证明线上识别结果可用
    ↓
用 Diff 判断问题发生在哪一层
    ↓
最后根据真实异常决定 Recovery Agent 的投入程度
```

当前第一优先级不是继续研究通用 Agent，也不是替换 Grounding 模型，而是完成详情页闭环采集和数据质量验证。只有这条链路跑通，后续模型本地化、异常 Agent 和生产部署才有可靠的评估基线。
