# 接入新平台指南

> 适用版本：WS-1 P3（2026-08-04，RidePricingFSM 已删除，计价采集由 YAML 子流程表达）
> 本文回答：**接入一个新打车 App（或新流程），哪些代码能复用、哪些要写、按什么顺序做。**

---

## 1. 一句话结论

**通用引擎 ~3100 行全部复用（零改动）；高德专属 ~1700 行是「一个平台的内容」，新平台以 v2 + 子流程为模板，主要工作是写流程 YAML + 少量平台 handler + 一个 profile JSON。**

```
collector/
├── cli/            # 参数解析/组装（通用，复用）
├── application/    # ExecutionContext：stats/等待/截图/标注（通用，复用）
├── workflows/      # FlowEngine + 流程原语（通用，复用）
├── domain/         # Platform 契约 / checkbox / vision 结果模型 / constraint（通用，复用）
├── infrastructure/ # ADB / VLMGrounder + domain.vision 适配器（通用，复用）
├── quality/        # state_verifier（通用，复用）
└── platform/
    ├── registry.py # 注册表（加一行注册）
    └── gaode/      # ← 高德专属内容（新平台在 platform/<name>/ 写自己的）
        ├── platform.py            # 平台工厂 + 专属 handler
        ├── flows/                 # 流程 YAML（导航 + 计价）
        ├── subflows/              # 可复用子流程（pricing_collect / detail_capture）
        ├── profiles/gaode.json    # 平台配置
        └── select_all / supplier_* / screenshot_organizer   # 高德 UI 专属逻辑
```

---

## 2. 可完全复用的通用能力（新平台一行不改）

| 能力 | 位置 | 说明 |
|---|---|---|
| 流程引擎 + 原语 | `collector/workflows/flow_engine.py` | `ground_click / input_text / scroll / scroll_until_visible / back / extract_list / for_each / loop_until / subflow / verify`；运行时状态 `{{.S.<key>}}` 模板；`ground_click` 支持 `ref_image / wait_after / cache_key` |
| 执行上下文 | `collector/application/context.py` | `ctx`：stats / 等待归因 / 截图重试与模式落盘 / 标注 debug 门控 / `ctx.vision` |
| 视觉接口 | `collector/domain/vision/` + `infrastructure/vision/` | `GroundingService / VisualQueryService / TextExtractor / RoiClassifier / ImageMatcher` 协议 + 结构化结果；`VLMServiceAdapter`（`ctx.vision`）统一 VLM 调用 |
| 设备层 | `collector/infrastructure/device/adb_utils.py` | 截图/点击/滑动/输入/返回/打开 App |
| 平台契约 | `collector/domain/platform.py` | `Platform` dataclass：flows_dir / profile_path / default_flow / add_cli_args / build_flow_vars / step_handlers |
| 注册表 | `collector/platform/registry.py` | `register_platform(...)` 一行注册 |
| 质量校验 | `collector/quality/state_verifier.py` | 像素 diff / 前台 Activity 校验 |
| 列表解析 | `collector/platform/gaode/supplier_parse.py` | `parse_suppliers_response`：JSON/逐行兜底 + 关键词过滤（**关键词应移到新平台 profile**） |

---

## 3. 新平台要写什么（最小集）

### 3.1 目录

```text
collector/platform/<name>/
├── flows/<flow>_<name>.yaml     # 流程（照 gaode v2 抄）
├── subflows/...                 # 计价等可复用子流程（可先复制 gaode 的再改）
├── profiles/<name>.json         # 平台配置
└── platform.py                  # build_platform() + 平台专属 handler
```

### 3.2 platform.py（照 gaode 抄，~40 行）

```python
"""<平台>接入描述"""
from __future__ import annotations
import argparse
from pathlib import Path
from collector.domain.platform import Platform

def add_cli_args(parser: argparse.ArgumentParser) -> None:
    parser.add_argument("--address", required=True, help="目的地")

def build_flow_vars(args, flow_name: str) -> dict[str, str]:
    return {"Address": args.address}   # 按流程需要加 Pickup 等

def build_platform() -> Platform:
    base = Path(__file__).resolve().parent
    return Platform(
        name="<name>",
        flows_dir=base / "flows",
        profile_path=base / "profiles" / "<name>.json",
        default_flow="v1",
        add_cli_args=add_cli_args,
        build_flow_vars=build_flow_vars,
        step_handlers={
            # 平台专属步骤：只有交互与通用原语不一致的才写
            # 如新 App 的「全选」「供应商列表识别」「结果命名」
        },
    )
```

### 3.3 流程 YAML（照 v2 抄）

```yaml
# flows/v2_<name>.yaml
name: "v2-<平台>-计价采集"
steps:
  - id: "open_app"
    type: "open_app"
    package: "com.example.app"      # ← 换成新 App 包名

  # 导航：换成新 App 的元素描述（首页搜索框/输入框/打车tab…）
  - id: "tap_taxi_tab"
    type: "ground_click"
    mandatory: true
    ground:
      element_desc: "…新 App 的打车入口描述…"

  # … input_text / ground_click 等，全部复用通用原语 …

  # 计价采集：直接引用通用子流程（若新 App 交互同高德）
  - id: "pricing_collect"
    type: "subflow"
    file: "../subflows/pricing_collect_<name>.yaml"
```

计价采集若交互与高德一致（勾选经济型 → 逐个供应商 → 详细计价规则），
**直接复制 `gaode/subflows/` 两个子流程**，只改 prompt / 参考图 / 关键词。

### 3.4 profile JSON

```json
{
  "platform": "<平台>",
  "package": "com.example.app",
  "timing": { "app_launch_wait": 3.0, "after_tap_wait": 2.0 },
  "steps": { "find_search_box": { "element_desc": "…" } },
  "collection": { "max_suppliers": 10 }
}
```

### 3.5 注册一行

```python
# collector/platform/registry.py
def _register_builtins() -> None:
    from collector.platform.gaode.platform import build_platform
    register_platform(build_platform())
    from collector.platform.<name>.platform import build_platform as _build  # ← 加这行
    register_platform(_build())
```

---

## 4. 计价采集如何复用（主场景）

`subflows/pricing_collect_gaode.yaml` 已是纯 YAML 原语，不含高德以外的硬编码：

```text
S0 上滑 → S1 全选经济(select_all) → verify
→ loop_until(pricing_loop_done)
    ├─ extract_list(s2_list_suppliers)   # 识别当前屏供应商
    ├─ for_each → subflow(detail_capture) # 逐个采集详细计价规则
    └─ scroll(下滑看更多)
→ pricing_result_organize(结果聚合)
```

- **交互一致**：直接复制子流程，只换 `_S2_PROMPT`、`detail_capture` 里的元素描述/参考图、`supplier_parse` 关键词。
- **交互差异大**（如没有「全选经济」、列表结构不同）：把对应步骤换成自己的平台 handler，通用循环/子流程骨架不动。

---

## 5. 验证清单（新平台）

1. **离线**：`python tests/test_pricing_collect.py`（含 v2 端到端）、`python tests/test_flow_primitives.py`、`python -m compileall collector tests`
2. **零侵入**：`tests/test_pricing_collect.py::test_fake_platform_zero_intrusion` 已演示「注册假平台 + 自定义步骤」不改通用代码
3. **真机**：`python -m collector.demo --platform <name> --flow v2 --address … --pickup … --adb-path … --vlm-api-key … --vlm-base-url …`
4. 重点检查：全选勾选、供应商列表识别、详细计价页滚动终止（「预约用车」）、结果聚合命名。

---

## 6. 常见问题

| 问题 | 处理 |
|---|---|
| 新 App 勾选方式不同 | 写自己的 `select_all` handler；`domain/checkbox` 的空间校验可复用，ROI 颜色阈值参数化进 profile |
| 列表不是「经济型栏」 | 改 `s2_list_suppliers` 的 prompt 与关键词；`parse_suppliers_response` 通用 |
| 滚动终止标记不同 | `detail_capture` 里改 `target_text` / `target_prompt` |
| 截图命名与 result 聚合不匹配 | `screenshot_organizer` 的命名规则参数化（tabs/关键字进 profile） |
| 需要本地 OCR / 位移估计 | 落地 `TextExtractor`（CAP-02）/ `ImageMatcher`（CAP-02/03），接口已在 `domain/vision` 定义 |
