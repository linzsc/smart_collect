# 采集耗时优化方案（2026-08）

> 目标：在保证「详细计价页 + 打车页」截图完整性的前提下，提高采集速度。
> 来源：2026-08-04 真机运行日志（v2 流程，--mode debug）。

## 1. 现状量化（每家供应商 S3 详表）

| 供应商 | 步骤总耗时 | API | 显式等待 | 截图/编码/设备 |
|---|---|---|---|---|
| 曹操出行 | 88.1s | 23.4s | 14.6s | ~50s |
| 火箭出行 | 81.1s | 21.8s | 14.1s | ~45s |
| 阳光出行 | 64.5s | 20.3s | 12.6s | ~32s |
| AA出行 | 76.5s | 22.6s | 13.6s | ~40s |
| 星徽出行 | 82.6s | 23.6s | 14.1s | ~45s |
| **平均** | **~78.6s** | **~22.3s** | **~13.8s** | **~42.4s** |

- 每家供应商约 12 次 VLM 调用（~22s）、~20 张截图、~14s 显式等待。
- 10 家供应商预计 13 分钟+（不含打车页导航/滑动）。

> 注意：计价采集 FSM 内部等待读的是 `profiles/gaode.json`（timing/collection），
> 不是 flow YAML 的 timing；改 flow YAML 只影响引擎导航步骤。

## 2. 优化方案

### 方案一：返回导航确定性化（PERF-02，已完成）

详细计价页 → 计价弹窗 → 打车页 的两次返回：

| 项 | 现状 | 改为 |
|---|---|---|
| 定位返回箭头 | 截图 + VLM ground（1 次 API/次） | 直接 `adb.back()`（确定性动作） |
| 返回后等待 | `after_tap_wait` 2.0s × 2 | `back_wait` 1.0s × 2 |
| 返回前截图 | 保留 | 保留（记录现场） |

收益：每供应商省 2 次 API（~4-8s）+ 2s 等待 ≈ 6-10s/家；10 家省 1-2 分钟。

### 方案二：确定性等待收敛（PERF-02，已完成）

| 等待点 | 现状 | 改为 |
|---|---|---|
| 点问号后 `pricing_page_wait` | 2.0s | 1.2s |
| 点「查看详细计价规则」后 `after_confirm_wait` | 3.0s | 1.5s |
| 点标签后 `tab_wait` | 1.0s | 0.8s |
| 返回后 `back_wait` | 2.0s | 1.0s |

收益：每供应商省 3-4s；10 家省 30-40s。

### 方案三：减少 VLM 调用（待确认）

- 问号定位并入 S2（S2 顺带返回坐标）→ 省 1 次/家。
- 休息日 tab 复用工作日 tab 的并列位置 → 省 1 次/家。
- 预计省 6-8s/家；需真机回归验证布局稳定性。

### 方案四：截图数量收敛（待确认）

- collect 模式下 `popup/detail_entry/detail_page/exit_to_*` 等过渡帧可不保存或合并。
- 预计省 2-3s/家；需确认过渡帧可砍。

### 方案五：标签坐标复用 + 详情页等待再收敛（PERF-03，已完成）

1. **标签坐标复用**：首次进入详细计价页时 LLM 记录「工作日/休息日」坐标并缓存，
   后续供应商直接复用坐标点击，不调 LLM、不截图 → 每供应商省 2 次 ground（~4-6s）。
2. **详情页动作等待再收敛**：

| 等待点 | 现状 | 改为 |
|---|---|---|
| `tab_wait`（点标签后） | 0.8s | 0.5s |
| `detail_scroll_wait`（详细页每次滑动后） | 0.5s | 0.3s |
| `scroll_top_wait`（回顶每次间隔） | 0.2s | 0.1s |
| `back_wait`（返回后） | 1.0s | 0.8s |

收益：每供应商再省 ~5-8s；10 家省 1-1.5 分钟。

## 3. 实测速度对比（2026-08-04，--mode debug）

| 阶段 | 每供应商平均 | API | 等待 |
|---|---:|---:|---:|
| 优化前 | 78.6s | 22.3s | 13.8s |
| PERF-02 后 | 63.7s | 15.8s | 9.2s |
| PERF-02+03 后 | **59.8s** | **11.8s** | **7.1s** |

- 较优化前：步骤 -24%，API -47%，等待 -49%。
- 注：本次日志屏 2 混入「北京的士/北京新出租」等出租车类运力商（已由 CAP-08 过滤修复），
  北京的士 空跑 20s 且未采到数据；修复后此类不再进入列表。

## 4. 执行记录

| 日期 | 任务 | 状态 | 说明 | 验证 |
|---|---|---|---|---|
| 2026-08-04 | PERF-03 | `DONE` | 方案五：标签坐标首次 LLM 记录后复用（不调 LLM/不截图）+ 详情页等待再收敛（tab_wait 0.5 / detail_scroll_wait 0.3 / scroll_top_wait 0.1 / back_wait 0.8） | test_pricing_collect（PERF-03 标签复用 + 全流程断言标签 ground=2）通过 |
| 2026-08-04 | PERF-02 | `DONE` | 方案一+二：返回确定性化 + 确定性等待收敛（back_wait 1.0 / tab_wait 0.8 / pricing_page_wait 1.2 / after_confirm_wait 1.5） | test_pricing_collect 全部通过；真实运行待验证 |
