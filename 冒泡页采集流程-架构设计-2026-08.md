# 冒泡页采集流程架构设计（UI 树版，仿照纯视觉方案）

> 分支：codex/ui-tree
> 原则：**完全仿照纯视觉流程**（`pricing_collect_gaode.yaml` 状态机，可成功运行），
> 仅把「识别 / 定位」能力从 LLM 换成 UI 树；「打开→进入冒泡页」和「全选经济」保持纯视觉。

---

## 一、总流程

```
阶段1：打开 App → 输入起终点 → 进入冒泡页        【纯视觉，不变】
阶段2：进入冒泡页后（本次设计）                  【UI 树 + 视觉混合】
```

## 二、阶段2 状态机（与纯视觉同构）

```
进入冒泡页
├─ ① 点左侧导航「经济」          UI 树（仅一次，锚定经济型列表开头）
└─ ② supplier_loop（loop_until，max_rounds=15）  每轮：
     a. 识别供应商（UI 树）→ 过滤 的士/出租/平台产品 → 带坐标
        · 维护 _pending 列表（已识别未采集，跨轮合并去重，刷新坐标）
        · 20% 余量过滤：center.y ∈ [0.2H, 0.8H] 才算「本轮可采」
     b. 分支：
        · 有可采 → for_each(max=1)：subflow 采集（含「勾选保证」+ 点问号）+ mark_processed
        · 无可采（空 或 全在边缘）→ s4_scroll 下滑（0.6→0.4，1/5 屏）
        · 下滑后回 a 重新识别
     c. 终止（loop_done）：
        · collected ≥ 10（max_suppliers），或
        · economy_ended 且 _pending 已采空（特快车/出租/优享 之上的采完），或
        · 滚动达上限仍无新增（兜底，防死循环）
```

## 二.5 单运力商勾选保证（替代「全选经济」，关键）

**需求**：只有被勾选的运力商行才有「?」——所以不要求"全选经济"，只要求**本次要采集的那个运力商被勾选**即可。

**UI 树可直接读行状态**（desc `车型:XX 已选/未选`，是直接状态，非推断）。

**顺序（先勾选 → 问号才出现 → 再点问号）**：
```
定位当前要采集的运力商行 → 读 desc
├─ 已选 → 直接定位「?」→ 点击
└─ 未选 → 点勾选框（行内最右 clickable，x≈1141）
         → 重新 dump → 重新定位行 → 确认 desc=已选
         → 定位「?」→ 点击
验证：实测未选行「?」节点不存在（问号区只有价格节点）；已选行才有「?」
```

## 三、20% 余量规则（新增）

- **安全带**：`center.y ∈ [0.2 × H, 0.8 × H]`（H = 屏幕高 2670）
- **识别到的运力商**：center 在安全带内才算「本轮可采」；在边缘（上下 20%）的行**保留在 _pending**，等滚动到中间再采（避免点到半截行/被遮挡行）
- **滚动手势**本身也在 [0.2, 0.8] 内（0.6→0.4），不贴边
- **问号点击坐标**由行坐标派生，天然继承同一余量约束

## 四、列表维护（每轮识别 + 去重 + 余量，与纯视觉一致）

```
_processed     : set[str]     已采集（跨轮去重）
suppliers      : list[str]    本轮「可采」= 未采集 ∧ 余量OK
_edge_suppliers: list[str]    本轮「边缘行」= 未采集 ∧ 贴边（待滚动后采）
round_had_suppliers: bool     suppliers 是否非空

实现说明：不跨轮保留 pending 列表——每轮重新 dump 识别当前屏（与纯视觉流程一致），
边缘行通过「下滑 → 重新识别」自然回到安全带后被采；_processed 保证不重复。
```

## 五、与纯视觉流程的映射（能力替换表）

| 纯视觉步骤 | UI 树方案 | 能力 |
|---|---|---|
| 打开→进入冒泡页 | 不变 | 视觉 |
| jump_economy（VLM ground_click 点「经济」） | `ui_tree_click rail_economy` | UI 树 |
| select_all（VLM 定位 + 像素 ROI） | **保持视觉（SEL-01 不变）** | 视觉 |
| s2_list（VLM S2 提示词） | `s2_list`（UI 树 desc 解析 + 过滤 + 余量） | UI 树 |
| q_ 点问号（VLM ground_click） | `ui_tree_click q_button` | UI 树 |
| s4_scroll（确定性下滑） | 不变（触发条件改为「本轮无可采」） | 确定性 |
| pricing_loop_done | 改为基于 _pending / collectable 判定 | 确定性 |

## 六、代码改动清单

1. `ui_tree_supplier.py`：新增 `is_safe_center(y, screen_h, margin=0.2)`；
2. `supplier_list.handle_s2_list_suppliers`：每轮识别 + `_processed` 去重 + 20% 余量过滤（`_edge_suppliers`）+ 日志带坐标与边缘标记；
3. `supplier_list.handle_mark_supplier_processed`：同步从 `_pending` 移除；
4. `supplier_list.handle_pricing_loop_done`：改用 `_pending` / collectable 判定终止；
5. `select_all`：**视觉主路径**（架构按视觉；当前 UI 树快速路径为决策点——保留作加速或回退）；
6. YAML：循环结构**基本不变**（s2 语义由 handler 承载），`s4_scroll` 触发条件语义改为「本轮无可采」；
7. 测试：余量规则、_pending 合并去重、边缘→滚动、终止条件（10 个 / economy_ended / 兜底）。

## 七、决策点（已确认）

- **移除「全选经济」作为流程前提**：改为**单运力商勾选保证**（见 二.5）——只确保本次要采集的行被勾选；
- **勾选状态读取**：UI 树直接读行 desc `已选/未选`（可靠），不依赖 `checked` 属性（该属性全为 false）也不靠推断；
- 现有 `select_all` 步骤：从冒泡页流程中移除（不再需要）；SEL-01 视觉实现保留供其他场景/回退使用；

## 八、验收

- 冒泡页内 S2/点经济/点问号 **0 LLM**（全选经济除外=视觉）；
- 采集顺序：同一运力商不重复；边缘行最终采到（滚动后进入安全带）；
- 终止：采满 10 个或经济段（特快车/出租/优享之上）采完，不死循环。
