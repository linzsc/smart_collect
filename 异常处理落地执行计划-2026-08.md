# 异常处理落地执行计划-2026-08

> 配套设计文档：《异常处理落地设计-2026-08.md》《StateVerifier与页面签名设计-2026-08.md》（均在 Obsidian Vault）
> 目的：把「异常处理」按可执行的子任务落地到代码。**每个阶段都是一个独立子任务**，按 `CLAUDE.md` 的"单对话单子任务"规则执行：一次只做一个、做完更新计划表、不自动开始下一个。

---

## 0. 总览（按依赖顺序执行）

| 阶段 | 动作 | 对应现有 TODO | 核心产出 | 是否涉及 LLM |
|---|---|---|---|---|
| P0-0 | 真机数据标定（不写代码） | — | 页面目录词表 + Activity 事实 | 否 |
| P0-1 | PageSignature + StateVerifier | VER-01 | 签名模型 + 5 条对比规则 + 单测 | 否 |
| P0-2 | 页面目录（Page Catalog） | VER-01 | 高德 8 页 YAML 配置 + 加载器 | 否 |
| P0-3 | YAML `verify` 支持 `expect_page` | VER-01 | `_do_verify` 扩展（向后兼容） | 否 |
| P0-4 | 替换 `_verify_page` 的 VLM 分类 | VER-01 | 确定性页面验证，每步省 1 次 VLM | 否 |
| P0-5 | 恢复原语库（确定性） | REC-01 前半 | 6 个原语 + 注册表 | 否 |
| P0-6 | 场景 C 最小切片（点问号偏移） | REC-01 | 勾选框修复 + 点问号后置验证 | 否 |
| P1-1 | 默认恢复链 FSM | REC-01 | 恢复链引擎 + zombie 防重试 | 否 |
| P1-2 | 失败指纹 + recovery_cache | 架构设计V2 | 指纹计算 + 缓存（先内存后 Redis） | 否 |
| P2-1 | Incident 记录 | REC-01 | Incident schema + 落盘 | 否 |
| P2-2 | recovery_agent（LLM 兜底） | REC-02 | 结构化生成 + 白名单校验 | 是（0–1 次/任务） |
| P3 | 冷却告警 + Temporal | — | 运维闭环（最后，可延后） | 否 |

**落地顺序原则**：P0/P1 是纯确定性改造（收益最大、不涉及 LLM）；P2 的 LLM 生成是"最后 1 公里"，等 L0–L2 稳定后再上。

---

## 1. 执行约定（每个阶段都遵守）

1. 先读现状和相关代码，不覆盖用户已有修改；
2. 所有循环有上限、外部调用有超时（`CLAUDE.md` 4.2）；
3. 普通测试不得调用真实设备和付费 API（用 mock / fixture）；
4. 完成一个阶段后：测试通过 → 更新 `CLAUDE.md`/`codex.md` 计划表对应任务为 `DONE`，并在开发记录追加一行；不自动开始下一个；
5. 如发现任务超出一个子任务，停止扩展，拆出剩余任务加入计划表。

---

## 2. P0-0 真机数据标定（不写代码，你现在就能做）

**目标**：产出页面目录需要的"词表 + Activity 事实"，让后面的签名设计有据可依。

**做什么**：
1. 真机采集关键页面截图，每页 5–10 张（覆盖不同供应商/价格/广告位/弹窗）：
   - 首页、搜索页、搜索结果页、POI 详情页、路径规划页、打车页、计价弹窗、计价页（共 8 类）；
2. 每页执行 `adb shell dumpsys activity activities | grep topResumedActivity`，记录**真实 Activity 全名**；
   - ⚠️ 重点确认：**首页 vs 打车页的 Activity 是否不同**。若相同（同 Activity 不同 fragment），则 Activity 不能当硬判别，必须靠 OCR；
3. 对每页跑本地 OCR（现有 `OcrTextExtractor`），统计候选关键词：
   - **页内命中率** = 该页出现该词的比例（期望 ≈100%）；
   - **串页误命中率** = 其他页出现该词的比例（期望 ≈0%）；
   - 例：打车页锁定「全选经济/经济型」（首页/其他页不出现）；首页锁定「搜索」。
4. 记录每个弹窗能否被 back 关闭（真机验证，供恢复链调参）。

**产出**：一张词表（每页 required/forbidden 词）+ Activity 对照表 + back 行为记录。

**验收**：能明确回答"首页和打车页靠什么区分"；词表可直接填进 P0-2 的页面目录。

---

## 3. P0-1 PageSignature + StateVerifier（第一个代码子任务，地基）

**目标**：把散落在 5 处的校验收口成唯一入口：`StateVerifier.verify(预期, 实际) → VerifyResult`，全部确定性、零 LLM。

**涉及文件**：`collector/quality/state_verifier.py`（现有 `verify_screen_diff` / `verify_activity` 保留为内部能力）；新增 `tests/test_state_verifier.py`。

**做什么**（按配套文档《StateVerifier与页面签名设计》§2/§3 实现）：

1. 数据模型：
   ```python
   @dataclass
   class PageSignature:
       activity: str | None = None      # 完整前台 Activity（dumpsys）
       package: str | None = None
       ocr_texts: tuple[str, ...] = ()  # 当前页 OCR 全部文本（可读，供 LLM/日志）
       pixel_hash: str | None = None    # 可选：感知哈希（只用于 stable 判断，不做身份）

   @dataclass
   class PageExpectation:
       page: str = ""                   # 页面名（页面目录 key）
       activity_prefix: str | None = None
       ocr_required: list[str] = ()
       ocr_forbidden: list[str] = ()
       stable: bool = False
       signature_region: list[int] | None = None  # 有效区域 bbox，排除共享 Tab 栏等

   @dataclass
   class VerifyResult:
       passed: bool
       reason: str = ""
       diff: dict | None = None         # 差异明细（供恢复链/指纹输入）
   ```
2. `StateVerifier.__init__(adb, ocr, screen_size, timeout=3.0)`；
3. `capture_signature(screenshot_path=None) -> PageSignature`：
   - Activity：复用 `verify_activity` 的 dumpsys 逻辑，改为返回完整 Activity 字符串；
   - OCR：`ocr.extract(screenshot_path)` 取全部 `blocks[].text`（复用 `OcrTextExtractor`/`ctx.ocr`）；
   - 像素：可选，用 `verify_screen_diff` 的像素能力做前后帧 stable 判定；
   - `signature_region`：OCR 文本只统计有效区域内（排除底部共享 Tab 栏/状态栏）。
4. `verify(expectation, screenshot_path=None, actual=None) -> VerifyResult`，5 条确定性规则：
   1. `activity_prefix` 不匹配 → FAIL（一票否决）；
   2. `ocr_required` 任一缺失 → FAIL（reason 指明缺哪个）；
   3. `ocr_forbidden` 任一出现 → FAIL（reason 指明出现哪个）；
   4. `stable=true` 且画面仍在变 → FAIL/继续等；
   5. 全过 → PASS。失败返回 `diff`（期望 vs 实际的差异明细）。
5. 保留 `verify_screen_diff` / `verify_activity` 签名（旧导入路径 `collector/state_verifier.py` 兼容层不动）。

**测试**（全部离线）：mock ADB（固定 dumpsys 输出）+ mock OCR（固定文本块）+ fixture 截图。
用例：① activity 匹配 + required 全在 → 通过；② activity 不匹配 → 失败；③ required 缺失 → 失败；④ forbidden 出现 → 失败；⑤ stable 未稳 → 失败/等待；⑥ 同输入两次运行结果一致（确定性回归）；⑦ 首页/打车页判别（用 P0-0 数据）→ 互不误判。**≥10 个用例**。

**验收**：同输入同输出；「打车页 vs 首页」用 P0-0 数据能正确区分；`verify_screen_diff`/`verify_activity` 旧调用不受影响。

---

## 4. P0-2 页面目录（Page Catalog）

**目标**：把"每个页面的预期签名"变成 YAML 配置（数据，不写死在代码），运营/QA 可维护。

**涉及文件**：新建 `collector/platform/gaode/page_catalog.yaml`；新建 `collector/quality/page_catalog.py`（加载 + 校验）。

**做什么**：
1. 页面目录（高德先 8 页，词来自 P0-0）：
   ```yaml
   pages:
     home:            # 首页
       activity_prefix: "com.autonavi.minimap"
       ocr_required: ["搜索"]
       ocr_forbidden: ["全选经济", "经济型"]
     ride_choose:     # 打车页
       activity_prefix: "com.autonavi.minimap/...RideChoose"   # 以 P0-0 真机为准
       ocr_required: ["全选经济", "经济型"]
       ocr_forbidden: ["搜索"]
       signature_region: [0, 0, 1080, 2000]   # 排除底部 Tab 栏（按真机分辨率标定）
     price_popup:     # 计价弹窗
       ocr_required: ["查看计价规则"]
     price_detail:    # 计价页
       ocr_required: ["预约用车", "计价规则"]
     # search / search_result / poi_detail / route_plan 同理
   ```
2. 加载器：`load_page_catalog(path) -> dict[str, PageExpectation]`；校验：activity_prefix 或词表至少一项、region 合法、无重复页面名；
3. 目录与平台 Profile 隔离（先高德，后续平台各自一份）。

**测试**：加载器单测（正常/缺字段/非法 region）；目录里的词与 P0-0 词表一致。

**验收**：目录加载 + 校验单测通过；8 页齐全。

---

## 5. P0-3 YAML `verify` 支持 `expect_page`

**目标**：让 SOP 步骤能用 `expect_page: 页面名` 触发签名验证，且旧写法不受影响。

**涉及文件**：`collector/workflows/flow_engine.py` 的 `_do_verify`（约 L809）。

**做什么**：
1. `_do_verify` 增加分支：
   ```yaml
   - id: "verify_ride_page"
     type: "verify"
     expect_page: "ride_choose"          # 从页面目录取 expectation
   ```
   → `StateVerifier.verify(...)`，失败抛 `StepFailed`（可被 `optional` 跳过）；
2. `expect_state` / `handler` / `activity_prefix` 三种旧写法保留（向后兼容）；
3. FlowEngine 构造时注入页面目录（`page_catalog_path` 参数，默认加载高德目录）。

**测试**：`tests/test_flow_primitives.py` 增加 expect_page 用例（mock adb/ocr）；旧 YAML 全量离线测试仍通过。

**验收**：新写法可用；旧流程零回归。

---

## 6. P0-4 替换 `_verify_page` 的 VLM 分类

**目标**：去掉 `ground_click.verify_page` 里每步 1 次的 VLM `classify_page`，换成确定性签名对比。

**涉及文件**：`collector/workflows/flow_engine.py` 的 `_verify_page`（约 L951）、`_do_ground_click`（约 L303）。

**做什么**：
1. `_verify_page(shot, expected)` 改为：`capture_signature(shot)` → `verify(expectation)` → 返回 `(passed, reason)`；不再调 `classify_page`；
2. `verify_page` 的 `expected` 字段语义从"VLM 分类名"升级为"页面目录名"（如 `ride_choose`）；
   - 旧 YAML（如 `v1_gaode.yaml` 的 `expected: "路径规划页"`）需要一个**旧名→目录名映射**或直接迁移 YAML；
3. 确认 `verify_page` 路径 `vlm_calls` 不增加（用 stats 断言）。

**测试**：mock 场景下 `verify_page` 路径 vlm_calls=0；打车页/首页判别用例通过。

**验收**：页面验证零 LLM；旧流程迁移后离线测试通过。

---

## 7. P0-5 恢复原语库（确定性）

**目标**：把 `_try_recovery` 里的内联策略抽成可注册、可单测的原语库。

**涉及文件**：新建 `collector/quality/recovery_primitives.py`；改 `collector/workflows/flow_engine.py` 的 `_try_recovery`（约 L959）。

**做什么**：
1. 原语统一签名：`(engine, ctx: dict) -> bool`（返回"是否已恢复到预期页"，**内部必须重新 verify，不 verify 不算成功**）：
   | 原语 | 动作 | 说明 |
   |---|---|---|
   | `retry` | 等待 N 秒后由调用方重试当前动作 | 时序抖动 |
   | `back` | `adb.back()` + 等待 + verify | 返回键关弹窗/回上一页 |
   | `home` | keyevent HOME + 等待 + verify | 回桌面 |
   | `relaunch_app` | `force_stop` + `open_app` + 等待 + verify | 崩溃/卡死/状态污染 |
   | `close_popup` | 截图 → OCR 找「关闭/X/我知道了/跳过/✕」→ 点击 → verify | 弹窗类（零 LLM） |
   | `re_navigate` | 执行指定子 SOP（复用 `_do_subflow`）→ verify | 路由分叉/进度丢失 |
2. 注册表 `PRIMITIVES: dict[str, Callable]`；YAML 恢复策略引用原语名；
3. `_try_recovery` 改为调用注册表；旧的 `vlm_find_and_click` / `back_retry` 保留为兼容策略；
4. 每个原语带**重试预算**（retry≤2、back≤3、relaunch≤2、re_navigate≤2）。

**测试**：每个原语单测（mock adb/ocr/verify）；`close_popup` 用"关闭/✕/我知道了/跳过"素材验证能找到并点击。

**验收**：原语库单测全过；`_try_recovery` 行为兼容。

---

## 8. P0-6 场景 C 最小切片（点问号偏移 → 勾选框被点掉）

**目标**：按文档「场景 C」落地"点问号 → 后置验证 → 未选中先勾选 → 再点问号 → 失败跳过"。

**涉及文件**：`collector/platform/gaode/select_all.py`（新增供应商行勾选框定位）、`collector/platform/gaode/subflows/detail_capture_gaode.yaml`、`collector/workflows/flow_engine.py`。

**做什么**：
1. **供应商行勾选框定位 locator**：复用 `select_all.py` 的 `locate_select_all_target_vlm` 模板，prompt 改为定位「{{supplier}}」行「预估」价格右侧的方形勾选框，返回 bbox（严格 JSON）；拿到 bbox 后 `crop_roi` + `classify_checkbox_roi` 判状态（复用 `checkbox.py`，确定性）；
2. **点问号后置验证**：`detail_capture_gaode.yaml` 的 `q_` 步骤加 `expect_page: price_popup`（计价弹窗签名：ocr_required=["查看计价规则"]）；
3. **恢复策略**（`on_page_mismatch` 或恢复链）：① 校验该运力商勾选框 → 未选中先点击重新选中 → 重验 CHECKED；② 重定位「?」点击 → verify(price_popup)；③ 预算内（①→②→③ ≤2 轮）仍失败 → 标记该运力商失败 + 记录现场 → **跳过继续下一个**（不中断整个任务）；
4. 顺带修正 `exit_detail`/`exit_popup` 的 back×2：确认在计价弹窗/计价页才 back，避免把打车页退掉（配合 P0-4 的 verify）。

**测试**：用"点偏掉勾选框"的素材/模拟流程验证能自动修复并进入计价页；失败路径能标记跳过。

**验收**：场景 C 端到端（mock）通过；`mark_supplier_processed` 在失败时也能执行（跳过不阻塞）。

---

## 9. P1-1 默认恢复链 FSM

**目标**：把原语串成确定性恢复链，带预算、每级 re-verify、zombie 防重试。

**涉及文件**：新建 `collector/quality/recovery_chain.py`；接入 `flow_engine._try_recovery`。

**做什么**：
1. 恢复链顺序：`retry(1–2) → back(≤3) → close_popup → home+relaunch(≤2) → re_navigate(≤2)`，每级后 `StateVerifier.verify`，通过即返回；
2. **zombie 防重试**：执行原语前先确认设备空闲 = Activity 稳定 + 连续 N 帧像素 diff < 阈值（复用 `verify_screen_diff`），否则等待；
3. 全部失败 → 返回"进入 L2/L3"信号（当前先记日志，L2/L3 后续阶段接）。

**测试**：场景 A（优惠券弹窗）端到端 0 LLM 恢复；每级预算耗尽即停（不死循环）。

**验收**：弹窗/时序/路由分叉三类异常在 mock 下由确定性链恢复，vlm_calls=0。

---

## 10. P1-2 失败指纹 + recovery_cache

**目标**：给"同类异常"一个稳定 ID + 缓存，实现"第二次起 0 LLM"。

**涉及文件**：新建 `collector/quality/recovery_cache.py`（指纹计算 + 缓存接口）。

**做什么**：
1. 指纹 = `sha256(抽象差异结构 | 失败动作)`，**对动态内容免疫**：
   - 输入用 `VerifyResult.diff` 的结构化字段（如 `required_missing=[全选经济]`、`forbidden_hit=[关闭词]`、`activity_ok=true`），**不要 hash 原始 OCR 文本**（否则"3.5 元红包/5 元红包"变两个指纹）；
2. 缓存接口：`get(fingerprint) / set(fingerprint, strategy) / invalidate(fingerprint) / fail_count(fingerprint)`；
   - key：`recovery:{fingerprint}`；value：恢复策略 JSON + 最近成功时间 + 成功次数；
   - 先**内存实现**（dict + TTL），接口兼容后续换 Redis；
3. 缓存治理：连续失败 ≥N 次 → 标记 stale（下次允许 LLM 重新生成）；App 大版本改版 → 全量失效（配合质检告警）。

**测试**：同一异常第二次命中缓存（0 LLM）；动态数字不碎片化指纹；连续失败触发失效。

**验收**：指纹稳定、缓存命中、治理生效，全部离线可测。

---

## 11. P2-1 Incident 记录（REC-01）

**目标**：每次恢复尝试都留痕，支撑审计与"恢复成功率"指标。

**涉及文件**：新建 `collector/quality/incident.py`；接入恢复链/`_try_recovery`。

**做什么**：
1. Incident schema：`id / 时间 / fingerprint / 页面(期望+实际) / 截图路径 / OCR / 尝试列表(手段+结果) / 最终结果 / 关联运力商`；
2. 落盘：`output/incidents/YYYYMMDD/{id}.json`；
3. **事中记录**：每次恢复尝试都 append（不只是失败后），否则"恢复成功率 ≥80%"没有分母。

**测试**：mock 一次恢复流程，验证 Incident 文件内容完整。

**验收**：失败/成功现场都能完整还原。

---

## 12. P2-2 recovery_agent（REC-02，LLM 兜底）

**目标**：L1+L2 都失败时，LLM 生成一次恢复策略并沉淀。

**涉及文件**：新建 `collector/quality/recovery_agent.py` + 提示词文件 + 校验器。

**做什么**：
1. 结构化输入：期望签名 + 实际签名 + 最近 3 步 + 已尝试恢复 + 现场截图 URL（复用 PageSignature 的可读字段）；
2. 结构化输出：动作序列（只允许原语库内的 action）+ fallback + confidence；
3. **白名单校验**：动作必须是原语库注册名；**禁止任意坐标点击/高危动作**（支付/登录/短信）；校验失败 → 不执行；
4. 校验通过 → 写 `recovery_cache` → 执行 → verify；失败则停止 + Incident + 告警；
5. 冷启动熔断：每任务/每天 LLM 异常调用次数上限，超限直接任务失败（避免成本波动）。

**测试**：mock LLM 输出合法/非法策略各一，验证白名单拦截与缓存写入。

**验收**：同异常第二次走 L2 缓存 0 LLM；非法策略被拦截。

---

## 13. P3 冷却告警 + Temporal（可选，最后）

- **失败冷却**：同指纹连续失败 ≥3 次 → 告警并停止该任务（防死循环）；
- **Temporal 集成**：任务级重试/持久化（恢复逻辑跑在 FlowEngine 内步骤级，Temporal 负责活动级兜底；恢复 Agent 也可做成 Activity）。此阶段可延后，不影响 P0–P2 价值闭环。

---

## 附录 A：现有代码速查（复用点）

| 文件 | 关键能力 | 在本计划中的用途 |
|---|---|---|
| `collector/quality/state_verifier.py` | `verify_screen_diff`（像素 diff）、`verify_activity`（dumpsys） | P0-1 的内部原语；zombie/stable 判定 |
| `collector/workflows/flow_engine.py` | `_do_verify`/`_verify_page`/`_try_recovery`/`_do_subflow`/`StepFailed`+`optional` | P0-3/4/5、P1-1 的改造点 |
| `collector/infrastructure/vision/ocr_adapter.py` + `ocr_client.py` | 滴滴内部 OCR，`TextExtractor` 协议 | P0-1 的 OCR 信号；`close_popup` 关闭词 |
| `collector/application/context.py` | `ctx.ocr` / `ctx.vision` / `ctx.stats`（vlm_calls/ocr_calls…） | 统一注入与度量 |
| `collector/infrastructure/vision/checkbox.py` | `classify_checkbox_roi` / `crop_roi`（本地蓝像素分类） | P0-6 供应商行勾选框校验 |
| `collector/platform/gaode/select_all.py` | `ensure_all_selected`（定位→ROI→点击→重验，SEL-01） | P0-6 复用其模式与 locator 模板 |
| `collector/platform/gaode/subflows/detail_capture_gaode.yaml` | 点问号→计价页流程 | P0-6 改造点 |
| `collector/domain/constraint.py` | `validate_bbox`（几何约束） | P2-2 白名单校验基础 |
| `tests/` | `test_flow_primitives.py` / `test_ocr_client.py` 等 | 每阶段加单测的位置 |

## 附录 B：建议的启动顺序

1. **P0-0（现在，不写代码）**：采真机数据 → 词表 + Activity 对照表；
2. **P0-1（第一个代码任务）**：StateVerifier + PageSignature 落地，配 ≥10 个离线单测；
3. 完成 P0-1 后，按 P0-2 → P0-6 → P1-1 → P1-2 → P2-1 → P2-2 依次推进；每完成一个在 `CLAUDE.md`/`codex.md` 计划表标记 `DONE` 并记录。
